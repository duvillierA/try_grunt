/*! try_grunt - v0.0.0 - 2013-11-11 */

//! preserveComments
function folderScript(a) {
    console.log("Hello, " + a);
}

folderScript("New user tt");

var test = "allo";

var func = function(a) {
    if (!a) return;
    return [ "route", "localStorage" ];
};

var bool = false;

func(bool);

var test = "new string var", arr = func(bool), i = 0;

while (arr.length > i) {
    i++;
}